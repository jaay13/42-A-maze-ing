[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A-Maze-ing maze generation engine"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
