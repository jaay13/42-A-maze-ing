WIDTH=15
HEIGHT=15
ENTRY=0,0
EXIT=14,14
OUTPUT_FILE=maze.txt
PERFECT=false
# Set SEED to get the same maze every run.
# Menu option 1 then rebuilds the same maze every time.
#SEED=42

# Set RENDERER to print it in ASCII format instead of blocks
#RENDERER=ascii

# Set ANIMATION_DELAY to change the solution reveal speed.
# Milliseconds per frame when showing the path; 0 turns it off.
#ANIMATION_DELAY=30
